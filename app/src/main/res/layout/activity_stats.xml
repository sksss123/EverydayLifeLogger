<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="kr.ac.kookmin.everydaylifelogger.StatsActivity">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:id = "@+id/mainText"
        android:text="Stats"
        android:textSize = "12pt"/>

    <ListView
        android:layout_width="match_parent"
        android:layout_height="200pt"
        android:id="@+id/statsList"
        android:layout_below="@id/mainText"></ListView>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="시간별"
        android:id="@+id/asTime"
        android:layout_below="@id/statsList"
        />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text = "종류별"
        android:id="@+id/asType"
        android:layout_below="@id/statsList"
        android:layout_toEndOf="@id/asTime" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text = "뒤로가기"
        android:id="@+id/statsBack"
        android:layout_below="@id/statsList"
        android:layout_toEndOf="@id/asType" />

</RelativeLayout>
